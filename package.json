{
  "name": "career-automation-platform",
  "version": "1.0.0",
  "description": "AI-powered career automation — resumes, cover letters, cold emails, job tracking",
  "private": true,
  "scripts": {
    "dev": "concurrently --names \"SERVER,CLIENT\" --prefix-colors \"cyan,magenta\" \"npm run dev:server\" \"npm run dev:fe\"",
    "dev:server": "cd server && npm run dev",
    "dev:fe": "cd fe && npm run dev",
    "install:all": "npm install && cd server && npm install && cd ../fe && npm install",
    "build:fe": "cd fe && npm run build"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "dependencies": {
    "@upstash/qstash": "^2.11.0",
    "cron-parser": "^5.5.0",
    "zod": "^4.4.3"
  }
}
